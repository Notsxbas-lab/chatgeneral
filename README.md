# chatgeneral

## Arranque rápido (Node portátil)

1. Ejecuta `chatgeneral/run-server-portable.bat`.
2. Abre `http://localhost:3000`.

Admin: contraseña `Paneladmin`.

## Arranque rápido (Node instalado)

```bat
cd chatgeneral
npm install
npm start
```

## Docker

```bat
cd chatgeneral
docker compose up --build
```
